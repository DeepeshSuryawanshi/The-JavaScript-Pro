// Extracts a part of a string between two specified indices (positions) and returns it as a new string.let text = "Hello, World!";
let text = 'Hello, world'
let part = text.substring(0, 5);
console.log(part); // "Hello"
